#include <stdio.h>
/**
 * msin - prints all single digit numbers of base 10 starting from 0
 *
 * Return (0);
 */
int main(void)
{ 
	int digits;

	for (digits = 0; digits <= 9; digits++)
		printchar('digits');

	printchar('\n);

	return (0);
